pub fn increment(n : f64) i32
{
    if (x) |INP| {
        x = x + 1;
    }
    else{
        y = y + 2;
        c = c + 4;
    }
    var a : i32 = 1;
    var b : i32 = 2 + 3;
}

pub fn increment(n : f64) i32
{
    if (x) |INP| {
        x = x + 1;
    }
    else{
        y = y + 2;
        c = c + 4;
    }
    var a : i32 = 1;
    var b : i32 = 2 + 3;
}
